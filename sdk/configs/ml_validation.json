{
  "tier": "tiny",
  "description": "Validation pin for the unplug-tiny span model (thresholds from data/catalog.toml)",
  "required_files": [
    "config.json",
    "tokenizer.json"
  ],
  "optional_weight_files": [
    "model.safetensors",
    "pytorch_model.bin"
  ]
}
